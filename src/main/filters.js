// TODO maybe remove mermaid
const mermaidFilter = {
  name: 'Mermaid chart',
  extensions: ['mmd', 'mermaid'],
}

const svgFilter = {
  name: 'Scalable Vector Graphics', extensions: ['svg'],
}

module.exports = {
  mermaidFilter,
  svgFilter,
}
